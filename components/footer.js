import React from "react";
import SectionHeader from "./section-header";
import styles from "./section.scss";

export default () => {
  return (
    <footer className={`flex footer`}>
      © Team wild — Adidas hackathon Amsterdam
    </footer>
  );
};
